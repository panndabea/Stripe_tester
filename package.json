{
  "name": "stripe-frontend",
  "version": "1.0.0",
  "description": "Minimal Stripe frontend (HTML/CSS/JavaScript)",
  "private": true,
  "scripts": {
    "start": "python3 -m http.server 8080 --directory frontend"
  }
}
